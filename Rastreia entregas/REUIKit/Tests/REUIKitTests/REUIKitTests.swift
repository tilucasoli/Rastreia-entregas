import XCTest
@testable import REUIKit

final class REUIKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(REUIKit().text, "Hello, World!")
    }
}
